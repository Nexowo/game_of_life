fn next(board : [Vec<Vec<usize>>; 1], size : usize)->[Vec<Vec<usize>>; 1] {
    count : u8 = 0;
    for i in 0..size {
        for j in 0..size {
            
        }
    }
}

fn main() {
    let size : usize = 5;
    let mut board = [vec![vec![0; size]; size]];
    board = next(board, size);
}